language: java
jdk:
- oraclejdk13
sudo: enabled
services:
  - mysql
before_install:
  - sudo mysql -e 'DROP DATABASE IF EXISTS bugsDb;'
  - sudo mysql -e 'CREATE DATABASE bugsDb;'
  - sudo mysql < "db.sql"
script: mvn clean verify

 